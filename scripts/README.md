# Scripts

| File | Purpose |
|------|---------|
| `check_briefing.mjs` | Checks Archilles briefing / marker text consistency |
| `archillator_desktop.py` | Optional Windows tray tool: hotkey → clipboard → Gemini → popup |

The desktop script is **not** the public web product and is not feature-parity with `index.html`.  
Install deps from the repo root: `pip install -r requirements.txt`.
