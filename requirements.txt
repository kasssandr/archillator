# Optional: Windows desktop helper only (scripts/archillator_desktop.py).
# Not required for the browser app. Python 3.11+.
#
#   pip install -r requirements.txt
#   python scripts/archillator_desktop.py

keyboard>=0.13.5
pyperclip>=1.8.2
requests>=2.28.0
pystray>=0.19.5
Pillow>=10.0.0
